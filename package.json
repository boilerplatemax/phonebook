{
  "name": "phonebook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "start": "node index.js",
  "dev": "node --watch index.js",
  "test": "echo \"Error: no test specified\" && exit 1",
  "build:ui": "cd frontend && npm install && npm run build && cd .. && rmdir /s /q dist && robocopy frontend\\dist dist /E",
  "deploy:full": "npm run build:ui && git add . && git commit -m uibuild || echo 'No changes to commit' && git push"
  }
  ,
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
